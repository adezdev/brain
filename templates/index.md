# index

Catalog of every known project. `deep page?` links to `projects/<name>.md` when one exists —
most won't at first, by design (see [[CLAUDE]]: pages get written on real investigation, not
guessed from a one-liner).

Update this file on every ingest, and append to [[log]] when you do.

## unclassified

| project | path | one-liner | deep page |
|---|---|---|---|
